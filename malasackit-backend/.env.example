# Email Configuration (Optional - for notifications)
# If not configured, the application will work but skip email notifications

# Gmail Configuration (Recommended)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
ADMIN_EMAIL=admin@your-domain.com

# Alternative Email Services
# For Outlook/Hotmail:
# EMAIL_SERVICE=outlook
# For Yahoo:
# EMAIL_SERVICE=yahoo
# For custom SMTP:
# EMAIL_HOST=smtp.your-domain.com
# EMAIL_PORT=587

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5173

# Instructions for Gmail Setup:
# 1. Enable 2-factor authentication on your Google account
# 2. Generate an "App Password" for this application
# 3. Use the app password (not your regular password) for EMAIL_PASS
# 4. Set your Gmail address as EMAIL_USER
# 5. Set admin email as ADMIN_EMAIL (can be same as EMAIL_USER)